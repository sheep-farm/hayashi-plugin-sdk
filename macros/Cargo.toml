[package]
name    = "hayashi-plugin-sdk-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for the Hayashi plugin SDK"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote        = "1"
syn          = { version = "2", features = ["full"] }
